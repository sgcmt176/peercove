# M0 計測レポート(G-7)

計測手順は README の「検証手順(G-7: 計測レポート)」を参照。
実測値を記入して M0 の納品物とする。

## 1. 計測環境

| 項目 | 内容 |
|---|---|
| 計測日 | (例: 2026-07-09) |
| Host | (例: Windows 11 / 自宅光回線 / ルーター型番) |
| Member A | (例: Ubuntu 22.04 VirtualBox ブリッジ / Host と同一 LAN) |
| Member B | (例: Windows 11 / モバイルテザリング=別 NAT) |
| コミット | (git rev-parse --short HEAD の値) |
| 備考 | (Tailscale 停止済み、ファイアウォール設定など) |

## 2. ゴール達成状況

| ゴール | 結果 | 備考 |
|---|---|---|
| G-1 トンネル作成・破棄 | ✅ / ❌ | |
| G-2 1対1 ping | ✅ / ❌ | |
| G-3 ハブ&スポーク ping | ✅ / ❌ | |
| G-4 TCP(curl) | ✅ / ❌ | |
| G-5 UDP(udp-ping) | ✅ / ❌ | |
| G-6 UPnP 開放 | ✅ / ❌ | 外部エンドポイント: |

## 3. RTT(ping 100 回の min/avg/max/mdev)

`ping -c 100 <宛先>`(Windows からは `ping -n 100`)

| 経路 | min (ms) | avg (ms) | max (ms) | mdev (ms) | 損失 |
|---|---|---|---|---|---|
| 【参考】A → Host 物理 LAN(トンネル外) | | | | | |
| A → Host(100.100.42.1) | | | | | |
| B → Host(100.100.42.1) | | | | | |
| A → B(100.100.42.3、**リレー経由**) | | | | | |

- リレーのオーバーヘッド(A→B avg − A→Host avg): ____ ms

## 4. スループット(iperf3、10 秒 × 3 回の中央値)

サーバー: Member A(`iperf3 -s`)/ クライアント: Member B

| 経路・条件 | 1回目 | 2回目 | 3回目 | 中央値 |
|---|---|---|---|---|
| 【参考】物理 LAN TCP(トンネル外、可能なら) | | | | |
| B → A TCP(リレー経由) | | | | |
| A → B TCP(`-R` 逆方向) | | | | |
| B → A UDP `-b 100M`(損失率も記録) | | | | |

- UDP 損失率 / ジッター: ____ % / ____ ms

## 5. udp-ping(トンネル内 UDP RTT)

`peercove-poc udp-ping --target 100.100.42.2:9999 --count 20`(B → A)

| min (ms) | avg (ms) | max (ms) | 損失 |
|---|---|---|---|
| | | | |

## 6. 所感・気付き

- (例: リレー経由の RTT 増分、CPU 使用率、MTU 由来の問題、
  Windows ユーザー空間実装のスループット上限など)
